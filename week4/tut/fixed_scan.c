#include <stdio.h>
#define NUM_DAYS 7;

int main() { 
  int temp[] = {0};

  int i = 0;
  while (i < NUM_DAYS) {
    scanf("%d", &temps[i]);
    i++;
  }

  int j = 0;
  while (j < NUM_DAYS) {
    printf("Temperature on day %d is %d", j, temp[j]);
    j++;
  }

  int k = 0;
  do {
    printf();
  }
  return 0;
}